<h1>Parts</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @parts.each do |part| %>
      <tr>
        <td><%= part.title %></td>
        <td><%= part.content %></td>
        <td><%= part.status %></td>
        <td><%= link_to 'Show', project_subject_parts_path(part) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
