<!DOCTYPE html>
<html>
  <head>
    <title>QuickLearning</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!-- Font Awesomeの読み込み -->
    <script src="https://kit.fontawesome.com/35325323f0.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome-animation/0.3.0/font-awesome-animation.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.7.2/animate.min.css">
  </head>

  <body>
    <% if (admin_signed_in? || user_signed_in?) && (current_admin.project_admin_projects.present? || current_admin.project_user_projects.present?) %>
    <div class="header">
      <div class="header-brand">Quick Learning</div>
      <div class="header-menu">
        <% if user_signed_in? %>
          <a class="icon fas fa-sign-out-alt faa-horizontal animated-hover fa-2x" href="/users/sign_out" data-method="delete" ></a>
        <% elsif admin_signed_in? %>
          <a class="icon fas fa-sign-out-alt faa-horizontal animated-hover fa-2x" href="/admins/sign_out" data-method="delete" ></a>
        <% else %>
          <a class="icon fas fa-sign-in-alt faa-horizontal animated-hover fa-2x" href="/users/sign_in"></a>
          <a class="icon fas fa-sign-in-alt faa-horizontal animated-hover fa-2x" href="/admins/sign_in"></a>
        <% end %>
        <a class="icon fas fa-user-plus faa-horizontal animated-hover fa-2x" href="/admins/sign_up"></a>
      </div>
    </div>
    <input type="checkbox" class="openSidebarMenu" id="openSidebarMenu">
      <label for="openSidebarMenu" class="sidebarIconToggle">
        <div class="spinner diagonal part-1"></div>
        <div class="spinner horizontal"></div>
        <div class="spinner diagonal part-2"></div>
      </label>
      <div id="sidebarMenu">
        <ul class="sidebarMenuInner">
          <li>Jelena Jovanovic <span>Web Developer</span></li>
          <li><a href="https://vanila.io" target="_blank">Company</a></li>
          <li><a href="https://instagram.com/plavookac" target="_blank">Instagram</a></li>
          <li><a href="https://twitter.com/plavookac" target="_blank">Twitter</a></li>
          <li><a href="https://www.youtube.com/channel/UCDfZM0IK6RBgud8HYGFXAJg" target="_blank">YouTube</a></li>
          <li><a href="https://www.linkedin.com/in/plavookac/" target="_blank">Linkedin</a></li>
        </ul>
    </div>
    <% end %>
    <% if admin_signed_in? && current_admin.project_admin_projects.present? %>
      <h5><%= link_to current_admin.name, current_admin %>/<%= link_to 'Project', project_path(current_admin.project_admin_projects.last) %></h5>
    <% end %>
    <% if user_signed_in? %>
      <h2><%= link_to 'Project', projects_path %></h2>/
      <h2><%= link_to 'my name', user(current_user) %></h2>
    <% end %>
    <% if admin_signed_in? && current_admin.project_admin_projects.present? %>
        <%= link_to 'Project', projects_path %>
        <%= link_to 'New Project', new_project_path %>

        <%= link_to "生徒を招待する", new_user_invitation_path %>
        <%= link_to "社員を招待する", new_admin_invitation_path %>
    <% end %>
        <%= yield %>
  </body>
</html>
